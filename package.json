{
  "name": "cs2-stats-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "prisma generate && next build",   // ← добавили prisma generate
    "vercel-build": "prisma generate && next build",
    "start": "next start",
    "postinstall": "prisma generate"            // ← на всякий случай для локалки/Vercel
  },
  "dependencies": {
    ...
  },
  "devDependencies": {
    ...
  }
}
